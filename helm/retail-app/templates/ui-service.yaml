apiVersion: v1
kind: Service
metadata:
  name: retail-app-ui
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: retail-app-ui
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: 8080
